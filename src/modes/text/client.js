export const mode = {
  name: 'Text',
  onCreate: () => {},
  onEnter: (event) => {},
  onExit: (event) => {},
  onSettingsChange: (settings) => {},
  events: {
    keydown: (event) => 'Same',
    keypress: (event) => 'Same',
    keyup: (event) => 'Same',
    focusout: (event) => 'Reset',
    focusin: (event) => 'Reset',
    click: (event) => 'Same',
    mousedown: (event) => 'Same'
  },
  messages: {}
};
