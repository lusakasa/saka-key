export const mode = {
  name: 'Text',
  onEnter: (event) => {},
  onExit: (event) => {},
  onSettingsChange: (settings) => {},
  events: {
    keydown: (event) => 'Same',
    keypress: (event) => 'Same',
    keyup: (event) => 'Same',
    blur: (event) => 'Reset',
    focus: (event) => 'Reset',
    click: (event) => 'Same',
    mousedown: (event) => 'Same'
  },
  messages: {}
};
