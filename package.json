{
  "name": "saka-action-test-extension",
  "description": "saka-action-test-extension",
  "scripts": {
    "build": "rimraf dist && webpack --config webpack.config.js && cp ./src/manifest.json ./dist/manifest.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "respository": {},
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.5.4",
    "webpack": "^2.2.1"
  },
  "dependencies": {
    "mosi": "0.0.13"
  }
}
